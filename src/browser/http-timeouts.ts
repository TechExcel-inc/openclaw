/**
 * HTTP client wait for browser control calls that block on navigation, rendering,
 * or large DOM (matches Project Run UI guidance for heavy sites).
 */
export const BROWSER_PAGE_LOAD_TIMEOUT_MS = 60_000;
